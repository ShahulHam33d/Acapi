"use client"; import Link from "next/link"; import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Signup(){const [loading,setLoading]=useState(false),[error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);if(d.get("password")!==d.get("confirm")){setError("Passwords do not match.");return}setLoading(true);try{const r=await fetch("/api/auth/dev-session",{method:"POST"});if(!r.ok)throw 0;router.push("/dashboard")}catch{setError("Unable to start the development session.");setLoading(false)}}return <main><h1>Create your workspace</h1><p>Development-only authentication</p><form onSubmit={submit}><label>Full name<input required/></label><label>Work email<input type="email" required/></label><label>Company name<input required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><label>Confirm password<input name="confirm" type="password" minLength={8} required/></label>{error&&<p>{error}</p>}<button disabled={loading}>{loading?"Creating workspace…":"Create account"}</button></form><Link href="/login">Sign in</Link></main>}
